================================================================================
ENDPOINT MEDIA — CONTENT CONSOLE (csmm / csmmendpoint)
================================================================================

WHAT THIS PROJECT IS
--------------------------------------------------------------------------------
A Streamlit-based internal tool for a social/content studio ("Endpoint Media").
It stores client brand profiles in SQLite, runs AI-assisted post generation
(CrewAI + Gemini) per client, and tracks manual distribution workflow: captions,
1:1 and 9:16 image prompts, optional overlay JSON, final exported images, QC,
and a separate "publisher" view for someone who posts to Facebook without
needing Meta page access.

Core idea: one place to generate and approve content, hand off assets + copy
to a publisher via a private link (query param + shared key), not auto-post
to Facebook in Phase 1.

KEY FILES
--------------------------------------------------------------------------------
  src/app.py          Main UI, theme CSS, navigation, generation hub, post library,
                      publisher queue view (?view=publisher&key=...)
  src/main.py         Entry: local runs spawn Streamlit on app.py; Cloud runs
                      app in-process (no nested Streamlit server).
  src/database.py     SQLite schema, migrations, posts/clients CRUD, QC/publisher
                      fields, content gap analysis helpers.
  src/seed_clients.py Default clients inserted when DB has zero clients (fresh deploy).
  src/crew.py         CrewAI social media crew wiring + `inject_engagement_insights`.
  src/engagement_learner.py  Posted + engagement metrics → patterns for research + Analytics.
  src/analytics.py    Plotly + `build_ai_learning_summary` (wraps engagement_learner).
  src/video_prompts.py  Short Video (9:16) format helper + `is_short_video_format`.
  src/content_calendar.py  Pillar colors, scheduled-date bucketing, monthly balance vs targets, **CSV/PDF month export**
                      (`export_month_to_pdf` / ReportLab), **batch auto-scheduling** (`auto_schedule_posts`,
                      `spread_schedule_dates`, `pillar_sequence_balance`).
  src/roles.py        URL `?role=` + session — `user_can(...)`, sidebar view sets (admin/copywriter/designer/publisher).
  src/asset_pipeline.py  Imagen pair + PIL overlay bake orchestration (used by Dashboard post cards).
  src/image_generation.py Imagen (Gemini API) single-image generation.
  src/overlay_pil.py   PIL heading/footer bands; parse overlay JSON; PNG or JPEG output.
  src/config/         brand_vault.py (tyre co-brands), vertical_creative.py (per-vertical
                      rules), agents.yaml, tasks.yaml (CrewAI task text + JSON contract).
  static/titan_ad_exporter.html   "Overlay studio" — browser HTML exporter (html2canvas).
  .streamlit/config.toml           Streamlit theme tokens (aligns with app CSS).
  agency.db           Local SQLite file (gitignored). Cloud gets its own ephemeral DB.
  .env.example        Documents env vars; real secrets go in .env (local) or
                      Streamlit Cloud Secrets (deployed). PDF export uses ReportLab only — no extra env vars.
  scripts/            upsert_* / add_client_* — programmatic client profile updates into agency.db

================================================================================
USER-FACING VIEWS (SIDEBAR NAVIGATION)
================================================================================

The left sidebar is the primary navigation. Structure:

  Endpoint Media          (title — Crimson Pro, large)
  Content studio          (caption — muted)

  [ Dashboard ]           Primary nav — generation + post library for selected client
  [ Analytics ]           Metrics + AI learning insights (role-gated)
  [ Content Calendar ]    Scheduled posts + placeholders (role-gated)
  [ New client ]          Onboarding form — add/edit client DNA (admin only)
  [ Overlay studio ]      Full-width embed of Titan-style HTML ad exporter (admin + designer)

  Role preview (admin only): dropdown + **Switch role** — writes `?role=` and session `em_role`.

  CLIENTS                 (section heading — uppercase, letter-spaced, muted)
  [ Client name ]         One button per client; active client highlighted (navy).
                          Clicking a client switches active client and returns to Dashboard.

Session state:
  - current_view: Dashboard | Analytics | Content Calendar | New client | Overlay studio
  - em_active_client_id: which client is selected for Dashboard generation/library
  - em_role: admin | copywriter | designer | publisher (also driven by `?role=` in the URL)

SPECIAL URL (NO SIDEBAR FLOW)
--------------------------------------------------------------------------------
  ?view=publisher&key=<PUBLISHER_SHARED_KEY>
  External publisher queue: posts marked QC "Ready for Publisher" for clients
  matching PUBLISHER_QUEUE_CLIENTS. Download ZIP, copy caption, track schedule/posted.

================================================================================
DASHBOARD LAYOUT (MAIN WORK AREA) — "Dashboard" VIEW
================================================================================

When "Dashboard" is selected and at least one client exists:

1) HERO (top)
   - Title: "Endpoint Media — Content console" (class ui-hero)
   - Subtitle explaining: pick client in sidebar, generate posts, copy assets,
     track delivery (class ui-hero-sub)

2) ACTIVE CLIENT STRIP
   - Line: "Active client: <name> · profile #<id>" (class ui-active-client)
   - Left accent bar in bronze (C_ACCENT); border and light surface background

3) CONTENT GAPS (last 30 days)
   - Heading + gap analysis alerts from database (pillars / featured brands vs recent posts)
   - Each alert: info/warning styling; optional "Generate Post to Fill Gap" button

4) GENERATION HUB (contained "card" region)
   - Wrapped in a bordered, shadowed container (em_generation_hub styling)
   - Typically includes:
     - Post format: Standard Feed (1:1) vs Story (9:16)
     - Content pillar, featured brand (vault), creative hook (options vary by vertical:
       automotive tyre vs battery vs firewood)
     - Batch count, mixed-format options, battery product line (battery vertical only)
     - Plan summary panel (em-panel): explains what will run for this client
     - Buttons: "Run generation" (primary navy), "Auto-Pilot (random settings)"

   Scheduling (advanced — admin & copywriter, inside Generation Hub)
   The **Advanced: Generate & Auto-Schedule Batch** expander runs CrewAI for 7–30 posts (optional mixed
   formats and balance-driven pillars), then assigns **scheduled_for** across the chosen date window via
   `content_calendar.auto_schedule_posts`. After a run, use **Go to Content Calendar** or **Stay here**
   to continue in the hub.

5) POST LIBRARY (below generation)
   - Section header styled as audit block (em-audit-header-*):
     "Post library" / subtitle about asset delivery & tracking
   - Summary table (Streamlit dataframe): post #, created, pillar, brand, format,
     delivery workflow status, QC status
   - Per-post cards (em-card):
     - Suggested caption (read-only text area) + Copy button (clipboard iframe)
     - 1:1 and 9:16 AI image prompts + Copy buttons
     - Optional expander: suggested text overlay JSON
     - Meta line: pillar, brand, format
     - Final images: upload square + vertical, Save, Mark ready for publisher,
       Revert QC to draft
     - Delivery status select: Draft / Sent to Client / Posted

================================================================================
"NEW CLIENT" VIEW
================================================================================
- Title: NEW CLIENT
- Two-column layout for Identity vs Market (columns with gap)
- Fields: company name, industry, services/products/brands, target markets,
  brand context, tone, photography style
- Initialize button creates client in DB and returns user to Dashboard behavior

================================================================================
"OVERLAY STUDIO" VIEW
================================================================================
- Title: Overlay Studio
- Embeds static/titan_ad_exporter.html via streamlit.components.html (tall iframe)
- Titan-style layout: dark panel, brand header, optional sub-text, frosted footer;
  export high-res JPEG via html2canvas (same as standalone HTML prototype)
- Note in UI: can open the HTML file directly in browser if CDN blocks

================================================================================
VISUAL DESIGN SYSTEM — COLORS (DESIGN TOKENS IN src/app.py)
================================================================================

Philosophy: "Classical editorial" — warm paper background, ink-like text,
restrained navy primary and bronze accent (not neon startup UI).

Token name              Hex         Role
--------------------------------------------------------------------------------
C_BG                    #f5f0e8     App page background (warm paper)
C_SURFACE               #fffcf7     Cards, inputs, sidebar lift, alerts
C_TEXT                  #2c2416     Primary ink (headings emphasis)
C_TEXT_BODY             #4a433a     Body copy
C_MUTED                 #6b6459     Captions, labels, secondary text
C_BORDER                #cdc6b9     Borders, dividers
C_INPUT_BG              #fffdf8     Input fields
C_PRIMARY               #1e3a5f     Navy — primary buttons, H2/H3, sidebar active
C_ACCENT                #7c5c36     Bronze — left accent bar on active client strip
C_HEADING               #1c1917     Hero / page titles
C_SUCCESS               #2f5f46     Success-adjacent (legacy name)
C_SHARK                 #ebe6dc     Panel background (plan summary panels)

Streamlit native theme (.streamlit/config.toml) mirrors these:
  primaryColor #1e3a5f, backgroundColor #f5f0e8, secondaryBackgroundColor #fffcf7,
  textColor #2c2416, base light, toolbar minimal.

================================================================================
TYPOGRAPHY
================================================================================

Fonts loaded from Google Fonts (injected in _inject_classical_theme):
  - Crimson Pro (serif): page titles, sidebar title, section headers, audit titles
  - Source Sans 3 (sans): body, labels, buttons, inputs, tables

Hierarchy:
  - Main h1: ~2rem, Crimson Pro semibold, C_HEADING
  - h2/h3/h4: Crimson Pro, C_PRIMARY
  - Body: 17px default, C_TEXT_BODY
  - Captions: ~0.92rem, C_MUTED
  - Sidebar section "Clients": uppercase micro-label, letter-spacing, C_MUTED

================================================================================
COMPONENT PATTERNS (CSS CLASSES)
================================================================================

em-card           Light surface card with border, 8px radius, subtle shadow
em-panel          Muted background (C_SHARK) plan/summary blocks
em-panel-label    Uppercase small label above panel body
em-panel-body     Panel body text
ui-hero / ui-hero-sub     Dashboard hero title + subtitle
ui-active-client  Active client row with bronze left border
em-audit-header-* Post library section titles (uppercase small title + serif name)
ui-sidebar-title   Sidebar app name
ui-sidebar-heading Sidebar section labels (e.g. "Clients")

Buttons:
  - Primary: navy fill (C_PRIMARY), hover slightly lighter blue (#2a4d75)
  - Secondary: surface fill, C_TEXT, C_BORDER outline
  - Border radius 6px, medium weight, Source Sans 3

Inputs:
  - Rounded 6px, C_INPUT_BG, C_BORDER, C_TEXT_BODY

Dataframe:
  - Bordered, rounded, C_SURFACE background

Progress / spinner:
  - Progress bar fill C_PRIMARY; spinner color C_PRIMARY

Clipboard buttons (inline HTML):
  - Light surface, navy text, border #cdc6b9 — matches editorial buttons

================================================================================
DATA & WORKFLOW (BEHAVIORAL MODEL)
================================================================================

Clients: company profile, industry, brand_context, tone, services_list,
         target_markets, photography_style

Posts:  caption, combined image_prompt (legacy), separate 1:1 and 9:16 prompts,
        suggested_text_overlay JSON, content pillar, featured brand, post format,
        workflow_status (Draft / Sent to Client / Posted),
        final image paths (square + vertical), qc_status (Draft / Ready for Publisher),
        publisher_status, scheduled_for, notes, timestamps.

Seeding: If the clients table is empty on init_db(), seed_clients.py adds
         Alberton Tyre Clinic, Alberton Battery Mart, Miwesu Fire wood, Absolute Offroad
         (full briefs in code — keep in sync with scripts/upsert_*.py).

Publisher: PUBLISHER_SHARED_KEY + PUBLISHER_QUEUE_CLIENTS (comma-separated name
           fragments) gate the external view.

================================================================================
WHERE WE ARE NOW (SPRING 2026)
================================================================================

Repo / deploy
  - GitHub: csmmendpoint (Streamlit app can auto-deploy from master).
  - Local run:  uv run python src/main.py  → Streamlit (usually http://localhost:8501).
  - Hosted: Streamlit Community Cloud — app URL is managed in Streamlit dashboard; waking
    from sleep redeploys from latest Git when Cloud rebuilds.

Environment
  - GEMINI_API_KEY required for generation (local .env or Streamlit Secrets).
  - PUBLISHER_SHARED_KEY + PUBLISHER_QUEUE_CLIENTS for external publisher view.

Database
  - agency.db is local; Cloud has its own SQLite (not in git). Client rows must exist on
    each environment (seed, onboarding UI, or scripts/upsert_*.py).

Clients in seed + onboarding (conceptual)
  - Alberton Tyre Clinic, Alberton Battery Mart, Miwesu Fire wood (seed when DB empty).
  - Absolute Offroad added via scripts/upsert_absolute_offroad.py and aligned seed_clients.
  - Overlay Studio presets: ATC, ABM, Miwesu, Absolute Offroad (static/titan_ad_exporter.html).

Recent behaviour changes (high level)
  - Absolute Offroad: offroad vertical — exact product naming in prompts, SA-market guardrails,
    attachment fidelity for product-reference images.
  - Alberton Battery Mart: diagnostics framed as health check on customer battery/charging system;
    Eco Plus / Power Plus mainly install/showcase/promo (not “new battery is faulty”);
    mix of in-store and studio/magazine-style ads allowed.

================================================================================
AI GENERATION MODEL — HOW EACH CUSTOMER / VERTICAL "THINKS"
================================================================================

This section explains the rules the app encodes so CrewAI output stays on-brand. The
"model" here is the combination of: CrewAI agents, task prompts (tasks.yaml), injected
vertical rules (vertical_creative.py), optional tyre brand vault (brand_vault.py),
and each client row in SQLite (brand_context, services_list, photography_style, etc.).

PIPELINE (same for all clients)
  1) User selects client + options in Dashboard (format, pillar, featured brand where
     applicable, creative hook, batch).
  2) App builds inputs: company profile + vertical_mode + vertical_creative_rules +
     research_vertical_hint + featured_brand / brand_guidelines / brand_models (tyres only).
  3) CrewAI runs sequential tasks:
     - market_researcher (research_task) — bullets grounded in client + pillar.
     - content_creator (content_creation_task) — ONE JSON object per run:
         Caption,
         Image_Generation_Prompt_1_1,
         Image_Generation_Prompt_9_16,
         Suggested_Text_Overlay { "Heading", "Footer" }.
  4) App saves post; images are produced manually from prompts (Phase 1).

VERTICAL ROUTING (src/config/vertical_creative.py)
  The app picks exactly one vertical per client. That controls which block of rules is
  injected and which creative hooks appear in the UI.

  vertical_mode values:
    firewood   — Miwesu (and similar if name/brief matches).
    battery    — Alberton Battery Mart (name match + battery keywords).
    offroad    — Absolute Offroad (name match) or strong 4x4/offroad keywords in brief.
    automotive — default: tyre retail (e.g. Alberton Tyre Clinic).

--------------------------------------------------------------------------------
AUTOMOTIVE (TYRE RETAIL) — e.g. Alberton Tyre Clinic
--------------------------------------------------------------------------------
How it "thinks":
  - Featured manufacturer (optional): dropdown uses brand_vault.py — slogans, visual rules,
    SA-oriented model hints for grounding copy (no invented tyre SKUs when co-branded).
  - Content pillar routes SETTING: promo/authority → clean studio hero; educational →
    minimal/macro; service → authentic workshop from photography_style.
  - Mechanical realism: wheel balancing shows tyre ON a rim (not loose carcass on balancer);
    alignment shows vehicle on rack or believable hardware — not floating tyre-only rigs.
  - Tyre condition logic: new tyres look new when being fitted; worn only when story needs it.
  - Demographics: if people appear and photography_style does not forbid faces, prompts ask
    for diverse, believable SA representation.
  - Image prompts: dual aspect (1:1 + 9:16), shallow DOF, overlay safe-zones for 1:1.

Hooks (examples): workshop, product hero, did-you-know, promo — tyre-appropriate list in app.py.

--------------------------------------------------------------------------------
BATTERY — Alberton Battery Mart
--------------------------------------------------------------------------------
How it "thinks":
  - NOT tyre retail: image prompts must not centre tyre tread, tyre-only heroes, or tyre
    workshop as the product story.
  - Workshop look when in-store: clean modern SA fit-out (light face brick, floated cement,
    charcoal feature walls) — from BATTERY_WORKSHOP_VISUAL_SPEC in vertical_creative.py.
  - Product lines: Eco Plus / Power Plus are priority for VALUE/showcase/install posts.
    Diagnostics/testing copy should focus on the CUSTOMER'S existing battery and alternator
    health — not “testing a brand-new Eco Plus/Power Plus because it is faulty.”
    New batteries appear in install, product hero, and promo contexts without fault implication.
  - Optional battery line picker in UI for targeted runs.
  - Scene mix: not every ad must be in-store — allowed: premium studio / magazine / gradient
    backdrops, product-on-plinth, campaign-style lighting (still photorealistic, no fake UI).

Hooks: battery-specific list (diagnostics, callout, Start/Stop, fleet, backup power, etc.).

--------------------------------------------------------------------------------
FIREWOOD — Miwesu Fire wood
--------------------------------------------------------------------------------
How it "thinks":
  - No tyre metaphors, no tyre vault co-branding for images/caption.
  - Wood imagery must follow SA hardwood xylology (Geelhaak, Knoppiesdoring, Sekelbos, etc.) —
    ban generic Northern pine/oak/birch look; details in MIWESU_WOOD_VISUAL_SPEC.
  - No phones, tablets, chat UI, or app screens in images — WhatsApp stays in caption CTA only.
  - Product names, bag sizes, MOQs, zones only from brief — no invented prices or suburbs.

Hooks: braai, delivery, product hero, ember/abstract heat, educational macro, etc.

--------------------------------------------------------------------------------
OFFROAD — Absolute Offroad
--------------------------------------------------------------------------------
How it "thinks":
  - Not tyre retail: tyre manufacturer vault and TYRE RETAIL block in tasks.yaml are skipped.
  - South Africa market guardrails: advertise products/brands that are credible SA-available
    per brief; no invented SKUs or international-only lines presented as in-stock.
  - Exact product lock: both image prompts must lead with a line like
    "Exact product to advertise: <one specific product string>" and the caption must repeat
    the same string — no vague "similar to EFS/Tough Dog" style-only wording.
  - Product ads with a user-supplied reference image: prompts repeat strict attachment
    fidelity — study attachment, match geometry/colour/branding, do not mutate the product.
  - Creative hooks are offroad-specific (suspension authority, protection, recovery, platform
    angles, etc.), not generic tyre hooks.

--------------------------------------------------------------------------------
SHARED JSON & QUALITY RULES (tasks.yaml — content_creation_task)
--------------------------------------------------------------------------------
  - Every run outputs valid JSON only (no markdown fences): Caption + two prompts + overlay;
    for **Short Video (9:16)** format, also **Video_Prompt** (9:16 motion brief for external video tools).
  - Captions end with contact block from brand_context (phones, WhatsApp, site, address rules
    per client), plus one line of 3–8 hashtags.
  - Dual prompts must match aspect (1:1 square vs 9:16 vertical) and usually share the same
    creative idea with recomposed framing.

================================================================================
LATEST UPDATE — 2026-03-28 (Phase 4 — item 4: Simple team roles / views)
================================================================================

- **`src/roles.py`:** Lightweight roles — **admin** (full), **copywriter** (Crew + captions + calendar + analytics;
  no Imagen/overlay bake), **designer** (prompts + Imagen + overlay + uploads + Overlay studio; no Crew/caption edits),
  **publisher** (external queue only). `?role=` syncs with `st.session_state["em_role"]`; **`?view=publisher&key=...`**
  forces publisher role.
- **Sidebar:** Nav buttons filtered per role; **admin** sees **Role preview** + **Switch role** (updates URL).
- **Dashboard:** “Working as …” line under active client; hub/packs/gap-fill/critic/engagement toggle respect permissions;
  post cards gate caption, Generate assets (Imagen vs prompts-only), QC, uploads, Tools, delivery status.
- **Publisher queue:** Caption notes role; existing shared-key gate unchanged.

Prior: Phase 4 items 1–3 (calendar, video prompts, engagement learner), Phase 3, vertical rules.

================================================================================
LATEST UPDATE — 2026-03-27 (Phase 4 — item 3: Deeper AI learning from engagement)
================================================================================

- **`engagement_learner.build_performance_summary`:** From **Posted** posts with recorded likes/reach,
  derives pillar reach totals, hook like-rates, 3–6 **winning_patterns** bullets, and recent high
  performers (caption snippets + metrics). Graceful copy when data is thin.
- **Crew loop:** `crew.inject_engagement_insights` adds `{engagement_insights}` to the market researcher
  task (`tasks.yaml`) when the hub toggle is on and at least three qualifying posts exist; otherwise
  injects an explicit “None” instruction.
- **Generation Hub:** Checkbox **Use past performance insights** (per client; default ON when enough
  metrics exist) applies to mixed pack, pillar pack, manual batch, gap-fill, Auto-Pilot, and Ctrl+G.
- **Analytics:** **AI Learning Insights** card (live snapshot + “Last learned on” timestamp) and optional
  standouts table.
- **Post card → Tools:** **Refresh AI Insights for this pillar/hook** stores a scoped snapshot in an expander
  (falls back to account-wide when the slice is small).

Prior: Phase 4 items 1–2 (calendar, video prompts), Phase 3, vertical rules.

================================================================================
LATEST UPDATE — 2026-03-26 (Phase 4 — item 2: Video prompt generation)
================================================================================

- **Short Video (9:16)** is a third post format in the Generation Hub and post library filter (alongside
  Standard Feed and Story). Crew outputs **Video_Prompt** in JSON when that format is selected; the
  creative critic refines it like image prompts. Stored in SQLite `posts.video_prompt`.
- **Dashboard post cards:** Read-only **Video prompt** block with copy; **🎥 Video** badge when a brief
  exists. **Generate assets** expander: optional **Generate Video Prompt** toggle, editable motion
  brief, **Save video prompt**, **Regenerate Video Prompt Only** (`VideoPromptOnlyCrew` — no Imagen rerun).
- **Analytics:** **Posts by format (last 90 days)** table; engagement scatter hover includes format.
- **Content Calendar:** Day list shows **🎥 Video** for Short Video rows or when `video_prompt` is set.
  **Export Month as CSV / PDF** (pick any of the three visible months): CSV via pandas; branded PDF via
  **ReportLab** (warm paper, navy header, balance box, scheduled-post table).

Prior: Phase 4 item 1 (Content Calendar grids, balance, placeholders), Phase 3, client review, assets,
vertical rules.

================================================================================
LATEST UPDATE — 2026-03-29 (Phase 5 complete — Production-ready v1.0)
================================================================================

**Status: Production-ready v1.0** — polish (Item 1), month export (Item 2), and batch generate +
auto-schedule (Item 3) are shipped.

- **Content Calendar exports:** **Month for export** + **Export Month as CSV** / **PDF** (ReportLab +
  pandas); disabled when the chosen month has no scheduled posts.
- **Generation Hub — Advanced: Generate & Auto-Schedule Batch** (admin + copywriter): choose **7 / 14 /
  21 / 30** posts, **Next 30 days** / **This calendar month** / **Next calendar month**, pillar mix
  **even** or **follow monthly balance** (`pillar_sequence_balance` — same deficit idea as Balance
  this month), optional **past performance insights** and **mixed formats** (Feed, Story, Short Video).
  **Run Batch & Schedule** calls `_execute_generation_pipeline` with `fixed_pillars`, then
  `content_calendar.auto_schedule_posts` + `update_post_scheduled_for` (noon UTC). Success banner with
  **Go to Content Calendar** / **Stay here**.
- **`_execute_generation_pipeline`** now supports `fixed_pillars` and `finalize_ui=False` for callers
  that finish UX after scheduling.

Prior: Phase 4 calendar/video, Phase 3 bulk/publisher, CrewAI + roles.

================================================================================
LATEST UPDATE — 2026-03-25 (Phase 4 — item 1: Content Calendar)
================================================================================

- **Content Calendar (sidebar):** New **Content Calendar** view for the active client — three-column
  monthly grids (**current month + next two**). Each cell is a button (`day` + post count); a second
  row shows **pillar-colored dots** (palette aligned to editorial accents in `content_calendar.PILLAR_HEX`).
  **Balance this month** compares **scheduled_for** posts in that month to scaled weekly targets
  (`DEFAULT_WEEKLY_TARGETS`: service 1, educational 2, promo 2, authority 1 per week).
- **Day expander:** Selecting a day lists scheduled posts; **Set scheduled date on draft** calls
  `database.update_post_scheduled_for` (noon UTC ISO); **Create placeholder post** uses
  `create_scheduled_placeholder_post` (draft + caption stub + prompts stub). Clears selection with
  **Clear day selection**. Gap cache bumps after schedule changes.

Prior batches (still true): Phase 3 (bulk actions, analytics, publisher ZIP), Feature 4/5, client
review, Generate assets, vertical rules.

================================================================================
FUTURE DIRECTION (NOT YET IMPLEMENTED)
================================================================================

Possible next releases (prioritize with stakeholders):

  1. **Video generation APIs** — Runway / Kling / Luma hooks from saved motion briefs (cost + QC gates).
  2. **Client review notifications** — WhatsApp or email deep links when approval stage changes.
  3. **Multi-client / agency views** — cross-client pipeline, shared calendars, workload by copywriter.
  4. **Usage & cost analytics** — Imagen/CrewAI runs per client, token estimates, export for finance.
  5. **Scheduling intelligence** — respect blackout days, platform-specific best-time hints, Meta API later.
  6. **Auth beyond `?role=`** — optional login, audit log, per-user activity.

================================================================================
END OF DOCUMENT
================================================================================
